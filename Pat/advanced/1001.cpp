#include <iostream>
#include <string>
#include <cmath>
#include <cstdlib>
using namespace std;

/*
int main()
{
    long long a = 0;
    long long b = 0;
    cin >> a >> b;
    long long c = a + b;
    if (abs(c) < 1000)
    {
        cout << c << endl;
    }
    else if (abs(c) < 1000000)
    {
        string str_c = to_string(c);
        long sz = str_c.size();
        cout << str_c.substr(0, sz-3) << "," << str_c.substr(sz-3, 3) << endl;
    }
    else
    {
        string str_c = to_string(c);
        long sz = str_c.size();
        cout << str_c.substr(0, sz-6) << "," << str_c.substr(sz-6, 3) 
             << "," << str_c.substr(sz-3, 3) << endl;
    }
    return 0;
}
*/